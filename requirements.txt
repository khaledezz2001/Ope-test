# requirements.txt is now only used for reference.
# All installs are handled in order inside the Dockerfile
# using `uv pip install --system` to avoid pip resolver conflicts.

runpod
pillow
opencv-python-headless
huggingface_hub
protobuf
numpy<2.0
pdf2image
compressed-tensors
qwen-vl-utils

# vLLM cu128 installed separately in Dockerfile via uv --torch-backend cu128
# transformers pinned to 4.48.3 (Qwen2Tokenizer compatibility with vLLM)
transformers==4.48.3

# olmocr without [gpu] extra — vLLM already owns torch
olmocr
