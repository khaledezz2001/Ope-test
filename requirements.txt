runpod
pillow
pdf2image
numpy<2.0
huggingface_hub
protobuf
qwen-vl-utils
compressed-tensors

# olmocr (no [gpu] extra — torch is in base image)
olmocr

# SGLang — replaces transformers for inference (continuous batching, prefix cache, cuda graphs)
# Install the nightly build for Blackwell (sm_120 / cu128) support
sglang[all] --find-links https://flashinfer.ai/whl/cu128/torch2.8/flashinfer-python
